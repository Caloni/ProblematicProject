#pragma once
#include <string>

/** Returns two concatenated strings with the string 'and' in the middle.
e.g. TwoStringsToStringWithAndInTheMiddle("left", "right") returns "left and right".
*/
std::string TwoStringsToStringWithAndInTheMiddle(std::string leftString, std::string rightString);

std::string ConcatenateTwoStrings(std::string firstString, std::string secondString);
